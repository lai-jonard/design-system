# Illustrations

Reusable UI illustrations referenced by components. These appear in more than one place.

| Slot | File | Used in |
|------|------|---------|
| Empty state — generic | `empty-generic.svg` | la-empty-state |
| Empty state — insights | `empty-insights.svg` | Legal area page, all clean |
| Empty state — documents | `empty-documents.svg` | Document list |
| Empty state — data room | `empty-data-room.svg` | Data room |
| Onboarding welcome | `welcome.svg` | Dashboard FTUE |

Drop SVGs here as they're created. Components reference them by path property.
