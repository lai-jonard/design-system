# Custom icons

SVG icons not covered by Phosphor Icons. Only add here if Phosphor genuinely doesn't
have what you need — check phosphoricons.com first.

Use these as `<img>` tags or inline SVGs in preview pages. For Lit components, pass
the path as a property.
